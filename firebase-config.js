// ---------------------------------------------------------------------------
// PASTE YOUR FIREBASE PROJECT CONFIG BELOW.
//
// Where to get it:
//   Firebase Console → Project settings (gear icon) → General →
//   "Your apps" → Web app → "SDK setup and configuration" → Config.
//
// These values are safe to commit to a public repo — they only identify your
// project. Access control is enforced by your Firestore security rules.
// See the README for setup steps and how to lock the rules down later.
// ---------------------------------------------------------------------------

export const firebaseConfig = {
  apiKey: "PASTE_API_KEY_HERE",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  storageBucket: "PASTE_PROJECT_ID.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId: "PASTE_APP_ID"
};

// Set to true once you've replaced the values above. The app shows a friendly
// warning until this flips to true so you know the config is still a stub.
export const firebaseConfigured = false;
